{
  "name": "firefox-mru-tab-switcher",
  "version": "0.1.0",
  "private": true,
  "description": "A Firefox MRU tab switcher with an Alt-Tab-like overlay.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "lint:webext": "web-ext lint --source-dir=src",
    "dev": "web-ext run --source-dir=src",
    "build": "web-ext build --source-dir=src --artifacts-dir=web-ext-artifacts --overwrite-dest"
  },
  "devDependencies": {
    "web-ext": "^10.4.0"
  }
}
